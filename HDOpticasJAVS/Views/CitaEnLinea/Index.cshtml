@model IEnumerable<HDOpticasJAVS.Cita>

@{
    ViewBag.Title = "Citas Agendadas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 90vh;">
    <div class="card shadow-lg w-100" style="max-width: 1100px; background: rgba(0, 0, 0, 0.75); color: #F1E0C6; border-radius: 15px;">
        <div class="card-header text-center" style="background-color: #1A73E8; color: white; border-top-left-radius: 15px; border-top-right-radius: 15px;">
            <h2 class="fw-bold" style="font-family: 'Poppins', sans-serif;">@ViewBag.Title</h2>
        </div>
        <div class="card-body" style="font-family: 'Roboto', sans-serif;">


            <div class="d-flex justify-content-between mb-3">
                <div>
                    @Html.ActionLink("Crear Nueva Cita", "Create", null, new { @class = "btn btn-success" })

                </div>
                <div>
                    <button class="btn btn-success me-2" onclick="exportarTablaExcel()">Exportar a Excel</button>
                    <button class="btn btn-outline-light" onclick="exportarTablaPDF()">Exportar a PDF</button>
                </div>
            </div>


            <div class="table-responsive">
                <table id="tablaCitas" class="table table-dark table-hover table-bordered">
                    <thead class="table-light text-dark" style="font-family: Raleway;">
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.Fecha_Cita)</th>
                            <th>@Html.DisplayNameFor(model => model.Hora_Cita)</th>
                            <th>@Html.DisplayNameFor(model => model.Empleado.Direccion)</th>
                            <th>Nombre cliente</th>
                            <th>Estado de la Cita</th>
                            <th>Especialidad</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Fecha_Cita)</td>
                                <td>@Html.DisplayFor(modelItem => item.Hora_Cita)</td>
                                <td>@Html.DisplayFor(modelItem => item.Empleado.Direccion)</td>
                                <td>@Html.DisplayFor(modelItem => item.Usuario.Nombre)</td>
                                <td>@Html.DisplayFor(modelItem => item.Parametro.Nombre_Parametro)</td>
                                <td>@Html.DisplayFor(modelItem => item.Parametro1.Nombre_Parametro)</td>
                                <td>
                                    <a href="@Url.Action("Edit", new { id = item.Id_Cita })" class="btn btn-warning btn-sm">Editar</a>
                                    <a href="@Url.Action("Details", new { id = item.Id_Cita })" class="btn btn-info btn-sm">Ver</a>
                                    @using (Html.BeginForm("Delete", "CitaEnLinea", FormMethod.Post, new { onsubmit = "return confirm('¿Desea eliminar esta cita?');", style = "display:inline;" }))
                                    {
                                        @Html.AntiForgeryToken()
                                        @Html.Hidden("id", item.Id_Cita)
                                        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Scripts de exportación -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.28/jspdf.plugin.autotable.min.js"></script>

<script>
    function exportarTablaExcel() {
        const tabla = document.getElementById("tablaCitas").cloneNode(true);

        const theadRow = tabla.querySelector("thead tr");
        if (theadRow) theadRow.removeChild(theadRow.lastElementChild);

        const filas = tabla.querySelectorAll("tbody tr");
        filas.forEach(fila => fila.removeChild(fila.lastElementChild));

        const wb = XLSX.utils.book_new();
        const ws = XLSX.utils.table_to_sheet(tabla);
        XLSX.utils.book_append_sheet(wb, ws, "Citas");

        XLSX.writeFile(wb, "Citas.xlsx");
    }

    async function exportarTablaPDF() {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();

        const tablaOriginal = document.getElementById("tablaCitas");
        const tablaClonada = tablaOriginal.cloneNode(true);

        tablaClonada.querySelector("thead tr").lastElementChild.remove();
        tablaClonada.querySelectorAll("tbody tr").forEach(row => row.lastElementChild.remove());

        doc.text("Listado de Citas", 14, 10);
        doc.autoTable({
            html: tablaClonada,
            startY: 20,
            styles: { fontSize: 10 },
            headStyles: { fillColor: [26, 115, 232] }
        });

        doc.save("Citas.pdf");
    }
</script>
